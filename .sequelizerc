const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'adapter', 'database', 'config', 'database.js'),
    'migrations-path': path.resolve(__dirname, 'adapter', 'database', 'migrations')
}